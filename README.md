# .dotfiles

This repository contains my dotfiles that I sync across all my sessions. The `setup.sh` script sets up symlinks for vim, git, tmux, bash, and matplotlib config, and installs ble.sh, fzf, and zoxide for an enhanced bash experience (no sudo required).
